<% layout("layouts/boilerplate") %>

<!-- Red Flash Effect (on wrong password) -->
<style>
  body.flash-error {
    background-color: #ff0000 !important;
    transition: background-color 0.2s ease;
  }
</style>
<% if (error) { %>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
        document.body.classList.add("flash-error");
        setTimeout(() => {
            document.body.classList.remove("flash-error");
        }, 300);
        });
    </script>
<% } %>

<div class="container d-flex justify-content-center align-items-start py-5" style="min-height: 80vh;">
  <div class="col-md-6 col-lg-5">
    <div class="card shadow rounded-4 border-0">
      <div class="card-body p-5">

        <h3 class="text-center mb-3 fw-bold">Confirm Password</h3>
        <p class="text-muted text-center mb-4">
          This post is protected. Please enter your password to continue.
        </p>

        <form method="POST" action="/posts/<%= post.id %>/pass">
          <div class="mb-3">
            <input
              type="password"
              name="password"
              class="form-control form-control-lg text-center"
              placeholder="Enter Password"
              required
            />
          </div>

          <div class="d-grid">
            <button type="submit" class="btn btn-primary btn-lg">
              Verify
            </button>
          </div>
        </form>

        <div class="text-center mt-4">
          <a href="/posts/<%= post.id %>" class="text-decoration-none text-muted">
            ← Cancel & go back
          </a>
        </div>

      </div>
    </div>
  </div>
</div>


<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>pass check</title>
    <style>
        .popup-error {
            position: fixed;
            top: 100px;
            left: 40%;                /* stick to top‑right */
            background-color: #f44336;         /* red bg */
            color: #fff;
            padding: 12px 20px;
            border-radius: 6px;
            box-shadow: 0 2px 8px rgba(0,0,0,.25);
            z-index: 9999;
            animation: hide 0.5s ease 2.5s forwards; /* auto‑hide */
        }
        @keyframes hide { to { opacity: 0; pointer-events: none; } }
    </style>
</head>
<body>
    <h2>Enter Password to Access Edit/Delete Options</h2>

    

    <form method="POST" action="/posts/<%= post.id %>/pass">
        <label for="password">Password:</label>
        <input type="password" name="password" required />
        <br/><br/>
        <button type="submit">Verify</button>
    </form>

    <% if (error) { %>
        <div class="popup-error"><%= error %></div>
    <% } %>

    <br/>
    <form action="/posts/<%= post.id %>" method="GET">
        <button>Cancel</button>
    </form>
</body>
</html> -->